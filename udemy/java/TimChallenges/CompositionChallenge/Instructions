Create a single room in the using composition.
Think about the things that would be included in the room.
Maybe physical parts of the room but furniture as well.
Add at least one method to access the object via getter and setter.
Then add at least one method to hide the object e.g. using a getter.
But to access the object used in composition within the main class.
